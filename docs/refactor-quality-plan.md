# 高价值等价重构与代码质量收口清单

- 状态：完成
- 日期：2026-08-08
- 基线：`github/main` at `1de458588de67fbfcc1f9be3c0cb69f70500e943`
- 边界：不改变业务行为、API、数据库 Schema、权限、审计、事务、回滚或运行数据。

本文件把本轮五个结构性任务包和代码质量收口列为显性任务。性能 P1–P5 另见
[`performance-high-yield-plan.md`](performance-high-yield-plan.md)。

## R1：拆分 `tests/test_app.py`

- [x] 按页面和业务域拆分 Web 集成测试，保留 `tests.test_app` 兼容入口。
- [x] 保留拆分前全部 143 个测试方法，禁止改名、漏测、重复发现或弱化断言。
- [x] 抽取测试启动基类和 Node 启动辅助逻辑，避免每个包装测试重复构造命令。
- [x] 增加本轮报价 DOM、产品目录与缺图行为的回归测试。

验收：拆分前测试方法在拆分后均且仅出现一次；测试发现、单文件兼容入口和全仓测试均通过。

## R2：拆分 `static/pages/inquiry_result.js`

- [x] 按调整、下载、图片、手工映射、产品选择和规则职责拆分模块。
- [x] 保留原入口路径、导出名称、初始化顺序、事件绑定和页面协议。
- [x] 扩充直接 Node 测试，覆盖拆出的纯函数和入口兼容行为。

验收：页面请求、DOM 标记、下载与写入顺序、特殊字符和错误处理保持一致；无新增控制台错误或资源 404。

## R3：拆分临界 CSS 和大型模板

- [x] 拆分共享 workspace/data-table/search/responsive 样式。
- [x] 拆分客户详情样式与模板分区。
- [x] 拆分用户账号、角色、权限和响应式样式/模板。
- [x] 保持现有 CSS 加载顺序、选择器语义、模板变量和表单字段。

验收：桌面和 390px 页面视觉、键盘操作、长文本、权限变体、资源加载和横向溢出均无回退。

## R4：拆分产品 Repository

- [x] 按目录、上下文、媒体、媒体事务、候选值、查询和写入职责拆分实现。
- [x] 保留 `app.modules.products.repository` 公共导入门面和现有返回类型。
- [x] 保留查询顺序、事务、审计、媒体失败补偿和产品同步消费者。

验收：产品 CRUD、目录导入、媒体、候选值、匹配、同步及类型/架构门禁全部通过；路由和 Schema 不变。

## R5：拆分业务同步 Infrastructure

- [x] 按归档、Schema、比较、预览、数据库应用和媒体事务职责拆分实现。
- [x] 保留原公共导入路径、包版本兼容、路径安全和资源上限。
- [x] 保留事务、全局导入锁、审计、备份、冲突处理和失败回滚。

验收：导出、预览、应用、媒体哈希、旧包兼容、异常关闭与失败补偿测试全部通过；不在当前运行数据执行导入。

## Q1：代码质量收口审计

- [x] 分别统计拆分前后全部生产源码和测试源码的物理行数、非空行数。
- [x] 检查重复实现、未使用导出、无调用方代码、过期兼容层、重复 CSS 和重复测试辅助逻辑。
- [x] 只删除有运行时、模板、API、CLI 和测试消费者证据支撑的低风险冗余。
- [x] 保留错误处理、审计、事务、回滚和测试强度；不通过压缩格式制造行数下降。
- [x] 把证据不足或兼容风险较高的候选写入
  [`code-quality-backlog.md`](code-quality-backlog.md)，不直接修改。

已实施的低风险精简：删除已下线且零消费者的旧产品同步 Service、零引用辅助符号、重复 Node 测试启动逻辑、客户列表误加载的详情页专用 CSS，以及已失效的遗留复杂度白名单项。

## 统一收口

- [x] 补充 ADR、项目简报和必要的 `changes/*.json` 变更片段。
- [x] 完成定向等价测试、性能基准和隔离数据库浏览器验收。
- [x] 自行 review 最终 diff，确认无权限泄漏、旧数据绑定、事务或回滚风险。
- [x] 运行 `uv run python scripts/verify.py --base-ref github/main`。
- [x] 使用 `bash tools/restart_local_5055.sh` 管理式重启并完成健康、就绪和页面探测。
- [x] 仅提交本轮范围内且已验证的改动；`.playwright-cli/` 保持未跟踪。
