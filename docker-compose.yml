services:
  bld-matcher:
    build: .
    container_name: bld-matcher
    restart: unless-stopped
    ports:
      - "5055:8000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
    environment:
      - TZ=Asia/Shanghai
